[package]
name = "demo-both"
version = { workspace = true }
edition = { workspace = true }

[features]
# Passthrough: enabling `test-reset` here turns on the library feature
# that mounts `POST /o2p/test/reset`. Only used by the Playwright E2E
# suite; never enable in production.
test-reset = ["oauth2-passkey-axum/test-reset"]

[dependencies]
oauth2-passkey-axum = { path = "../oauth2_passkey_axum" }
#oauth2-passkey-axum = "0.2"

askama = { workspace = true }
axum = { workspace = true }
dotenvy = { workspace = true }
http = { workspace = true }
serde = { workspace = true }
subtle = { workspace = true }
tokio = { workspace = true }
tracing = { workspace = true }
tracing-subscriber = { workspace = true }
