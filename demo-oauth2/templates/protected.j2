<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Protected Area</title>
    <style>
        .user-info {
            background: #f5f5f5;
            border-radius: 8px;
            padding: 1rem;
            margin: 1rem 0;
        }
        .user-info td {
            padding: 0.5rem;
            border-bottom: 1px solid #ddd;
        }
        .user-info td:first-child {
            font-weight: bold;
        }
    </style>
</head>
<body>
    <h1>Welcome to the Protected Area</h1>
    <div class="user-info">
        <h2>Your Account Information:</h2>
        <table style="width: 100%; border-collapse: collapse;">
            <tr>
                <td>User ID</td>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <td>Account</td>
                <td>{{ user.account }}</td>
            </tr>
            <tr>
                <td>Label</td>
                <td>{{ user.label }}</td>
            </tr>
            <tr>
                <td>Created At</td>
                <td>{{ user.created_at }}</td>
            </tr>
            <tr>
                <td>Updated At</td>
                <td>{{ user.updated_at }}</td>
            </tr>
        </table>
    </div>

    <p><a href="{{ auth_route_prefix }}/oauth2/logout">Logout</a></p>
</body>
</html>
