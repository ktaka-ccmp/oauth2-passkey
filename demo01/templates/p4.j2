<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Protected Area</title>
    <style>
        .user-info {
            background: #f5f5f5;
            border-radius: 8px;
            padding: 1rem;
            margin: 1rem 0;
        }

        .user-info dt {
            font-weight: bold;
            margin-top: 0.5rem;
        }

        .user-info dd {
            margin-left: 1rem;
        }
    </style>
</head>

<body>
    <h1>Welcome to the Protected Area</h1>
    <div class="user-info">
        <h2>Your Account Information:</h2>
        <table style="width: 100%; border-collapse: collapse;">
            <tr>
                <td>User ID</td>
                <td>{{ user.session_user.id }}</td>
            </tr>
            <tr>
                <td>Account</td>
                <td>{{ user.session_user.account }}</td>
            </tr>
            <tr>
                <td>Label</td>
                <td>{{ user.session_user.label }}</td>
            </tr>
            <tr>
                <td>Created At</td>
                <td>{{ user.session_user.created_at }}</td>
            </tr>
            <tr>
                <td>Updated At</td>
                <td>{{ user.session_user.updated_at }}</td>
            </tr>
        </table>
    </div>

    <button onclick="Logout()">Logout</button>
    <script>
        function Logout() {
            window.location.href = "{{prefix}}/user/logout?redirect=/";
        }
    </script>
</body>

</html>
